{
  "name": "@switchback/ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./theme.css": "./theme.css"
  },
  "//dependencies": "Deliberately none. This package is imported by a Next.js server bundle, a browser bundle, and a Hermes bundle, so anything it depends on has to work in all three. Tokens are values; keeping the dependency list empty is what makes that true rather than hopeful."
}
