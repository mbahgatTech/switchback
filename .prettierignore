node_modules
package-lock.json

# builds
dist
.next
.next-*
out
.expo
apps/mobile/ios
apps/mobile/android

# test and tool output. Prettier would happily reformat a Playwright report — a single-line
# HTML file with megabytes of embedded base64 in it — and the result is both slow and thrown
# away by the next run.
coverage
playwright-report
test-results
blob-report
.design
.impeccable

# local caches and user content, not source
.cache
tmp
.uploads
*.pmtiles

# generated by the frameworks on every dev/build run — formatting it is a diff that comes
# straight back the next time either tool regenerates the file
apps/web/next-env.d.ts
apps/mobile/expo-env.d.ts

# vendored third-party code, served verbatim from public/. Reformatting it would break the
# only guarantee it has — that it is byte-identical to the published tarball whose digest
# `src/components/map/rtl.ts` records.
apps/web/public/vendor
