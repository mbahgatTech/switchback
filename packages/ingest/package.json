{
  "name": "@switchback/ingest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "//pngjs": "Pure JS, no native binary. sharp is already in the tree and would decode faster, but ingest runs inside a Vercel function and inside vitest, and a 256x256 terrarium tile is 64 KB — the decode is not the bottleneck, the network is. A pure-JS decoder keeps this package installable anywhere without a platform-specific build.",
  "dependencies": {
    "@switchback/core": "*",
    "@switchback/db": "*",
    "@switchback/geo": "*",
    "pngjs": "^7.0.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/pngjs": "^6.0.5"
  }
}
